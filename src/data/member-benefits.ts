export interface MemberBenefit {
  provider: string;
  logo?: string;
  description: string;
  benefit: string;
}

export const memberBenefits: MemberBenefit[] = [
  // Placeholder - populate with actual benefit providers when available
];
